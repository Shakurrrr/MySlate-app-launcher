<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myslates.launcher">

    <queries>
        <!-- Allow querying these specific packages -->
        <package android:name="com.ATS.MySlates" />
        <package android:name="com.ATS.MySlates.Parent" />
        <package android:name="com.ATS.MySlates.Teacher" />
        <package android:name="com.adobe.reader" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/myslate"
        android:roundIcon="@mipmap/myslate_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySlateAppLauncher">


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MySlateAppLauncher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
