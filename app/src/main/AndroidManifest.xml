<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myslates.launcher">

    <!-- Needed for relaunch after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      DO NOT request privileged/system permissions in a normal app.
      These can cause "APK can’t be installed":
      - android.permission.WRITE_SECURE_SETTINGS        (signature)
      - android.permission.MANAGE_USERS                 (signature)
      - android.permission.INTERACT_ACROSS_USERS_FULL   (signature)
      - BIND_DEVICE_ADMIN is specified on the receiver element (not as a uses-permission)
    -->

    <queries>
        <package android:name="com.ATS.MySlates" />
        <package android:name="com.ATS.MySlates.Kids" />
        <package android:name="com.adobe.reader" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/myslate"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/myslate_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySlateAppLauncher">

        <!-- Launcher (Home) activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:clearTaskOnLaunch="true"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.MySlateAppLauncher"
            android:excludeFromRecents="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Device Admin receiver used when setting Device Owner -->
        <receiver
            android:name=".LauncherDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Start launcher on boot -->
        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
