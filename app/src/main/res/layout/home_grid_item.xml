<?xml version="1.0" encoding="utf-8"?>
<com.myslates.launcher.SquareTileLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="4dp">

    <!-- Empty Slot Indicator -->
    <View
        android:id="@+id/empty_slot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/empty_slot_bg"
        android:alpha="0.3"
        android:visibility="visible" />

    <!-- App Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp">

        <ImageView
            android:id="@+id/home_grid_icon"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:background="@drawable/modern_icon_bg"
            android:clipToOutline="true"
            android:elevation="6dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/home_grid_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:alpha="0.9"
            android:visibility="gone" />

    </LinearLayout>

</com.myslates.launcher.SquareTileLayout>
